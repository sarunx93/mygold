import { makeStyles } from "@mui/styles";
export default makeStyles(() => ({
  title: {
    flex: 1,
    color: "gold",
    fontFamily: "Montserrat",
    fontWeight: "600",
    cursor: "pointer",
  },
}));
